  language: python
  python:
    - "3.4"
    - "3.5"
    - "3.6"
    - "3.7-dev"
  install:
  - pip install -r requirements.txt
  compiler:
    - gcc
  script:
    - python setup.py build
    - pip install .
    - pytest tests/
